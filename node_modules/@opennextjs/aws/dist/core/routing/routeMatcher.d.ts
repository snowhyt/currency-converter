import type { RouteType } from "../../types/open-next";
export declare const apiPrefix: string;
export declare const staticRouteMatcher: (path: string) => {
    route: string;
    type: RouteType;
}[];
export declare const dynamicRouteMatcher: (path: string) => {
    route: string;
    type: RouteType;
}[];
