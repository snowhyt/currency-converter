interface CopyTracedFilesOptions {
    buildOutputPath: string;
    packagePath: string;
    outputDir: string;
    routes: string[];
    bundledNextServer: boolean;
    skipServerFiles?: boolean;
}
export declare function copyTracedFiles({ buildOutputPath, packagePath, outputDir, routes, bundledNextServer, skipServerFiles, }: CopyTracedFilesOptions): Promise<void>;
export {};
